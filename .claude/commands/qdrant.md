Query the project-docs Qdrant vector database using the user's question.

Run this bash command and show the results:

```bash
python3 /home/maria/Desktop/shop/proshop_mern/project-data/query.py "$ARGUMENTS"
```

Present the results clearly: for each result show the score, source file, section, and the text. Summarize what the top results say about the user's question in 2-3 sentences.
